# 著作権のある歌詞が描かれたテスト画像。公開しない
.claude/*.png
.claude/*.jpg
# テスト画像の正解データ（画像と対で使うもの。画像を入れないので一緒に外す）
.claude/ocr-*.json

# 使っていない高精度モデル（効果が無かったので採用せず）
vendor/tessdata-best/

# OS・エディタ
Thumbs.db
desktop.ini
.DS_Store
*.bak

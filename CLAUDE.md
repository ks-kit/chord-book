# コード帳 — 弾き語り用コード譜アプリ

iPhone で見る前提の PWA。**コード譜をコピペして貯めて、見やすく表示する**のが目的。
2026-09-12 に第1弾を作成。

## 何ができるか

| 機能 | 内容 |
|---|---|
| **取り込み** | コード譜をそのまま貼り付け → 「歌詞の上にコード」形式を自動解析。`[Intro]`『【サビ】』などのセクション、`\|` `x2` `N.C.` も認識 |
| **画像から読み取る（OCR）** | スクショを貼り付け（Ctrl+V）またはファイル選択。**端末内だけで処理し、画像はどこにも送らない**。複数枚を続けて読むと下に足していく |
| **曲名・アーティストの自動取得** | 画像上部から拾う。**曲名は文字が大きい**という手がかりで判別し、譜面より前の行（作詞・作曲等）は本文から外す |
| **曲名のネット照合** | Apple の楽曲検索で正式な曲名・アーティスト名に直せる。**ボタンを押した時だけ**送信、送るのは曲名とアーティスト名だけ |
| **重複の除去** | 複数枚を読むとき、前の画像の末尾と重なる行を落とす。**3行以上一致した時だけ**、落とした行数を必ず表示 |
| **直す箇所への案内** | 読めなかった語と疑わしいコードをボタンで並べ、**タップすると本文のその箇所が選択される**。そのまま上書きで直せる |
| **自動抽出** | **ページごと全選択してコピーして貼ってOK。** メニュー・広告・関連曲などを捨てて譜面部分だけ残し、曲名とアーティストも拾う。効きすぎた時は「貼ったままに戻す」で元に戻せる |
| **表示** | 歌詞の上にコードを配置。全角・半角の桁を計算して位置を合わせる |
| **押さえ方** | コード名を**タップした時だけ**ポップアップ。メニューから「使うコードを一覧」も出せる |
| **転調** | 半音単位で上下。キー名（鳴るキー）を表示。♯/♭はキーの慣用表記に自動で合わせる（+3 なら D# ではなく Eb） |
| **カポ** | 押さえるコードだけが下がる。鳴るキーは変わらない。開放弦で弾けるカポ位置を提案することがある |
| **メトロノーム** | 曲ごとに BPM と拍数を保存。数値入力・±・**タップテンポ**（叩いた間隔から自動計算）。Web Audio の先読み予約でリズムがよれない |
| **端末間の同期** | Dropbox のアプリ専用フォルダに `songs.json`。PKCE（秘密鍵なし）。曲ごとに新しいほうを採用、削除は削除印で伝える |
| **その他** | 文字サイズ7段階／一定速度のオートスクロール／画面スリープ防止（Wake Lock）／ダーク・ライト切替／JSON バックアップの書き出しと読み込み |

曲データは **端末内の localStorage** に入る。Dropbox と接続すれば端末間で同期する（手順は README）。

## 意図して作っていないもの

第1弾では「コード譜が見られればいい」という方針で、以下は**あえて入れていない**。

- 拍のタップ入力による原曲同期（1曲3分の仕込みが必要なため、使ってみて欲しくなったら追加する）
- 区間ループ、テンポ変更、練習記録
- コード譜サイトからの自動取得（利用規約に触れるため、コピペで運用する）

## ファイル

| ファイル | 役割 |
|---|---|
| `index.html` | 画面の骨組み（ライブラリ／編集／表示の3画面＋シート類） |
| `app.js` | 本体。保存、画面遷移、転調・カポ、オートスクロール |
| `chords.js` | コード名の解析・移調・押さえ方図（SVG）の生成 |
| `parser.js` | 貼り付けたテキストの解析（コード行判定、桁→歌詞位置の変換） |
| `ocr.js` | スクショのOCR。行の切り出し・コード名の補正・桁の復元 |
| `metronome.js` | メトロノームとタップテンポ |
| `sync.js` | Dropbox 同期（認証・読み書き・マージ） |
| `app.css` | 見た目 |
| `vendor/` | tesseract.js 本体（LSTMコア3種）と言語データ（eng/jpn の fast）。合計約15MB |
| `sw.js` / `manifest.webmanifest` / `icon-*.png` | PWA まわり |
| `.claude/devserver.py` | 開発用のキャッシュ無効サーバ |

## 開発中の動作確認

```bash
python .claude/devserver.py 8766
```

ブラウザで `http://localhost:8766/` を開く。
**普通の `python -m http.server` だとブラウザが JS をキャッシュして古いコードのまま動く。** 必ずこちらを使う。

## iPhone で使うには（重要）

**ネット公開が必要。** ローカルファイルは iPhone の Safari で開けず、ホーム画面にも追加できない。
カメラアプリ（`..\カメラアプリ`）と同じく GitHub Pages に置くのが一番楽。公開したら Safari で開き、共有ボタン →「ホーム画面に追加」。

**公開済み（2026-09-13）**

- アプリ: https://ks-kit.github.io/chord-book/
- リポジトリ: https://github.com/ks-kit/chord-book（public）

Safari で開いて 共有 →「ホーム画面に追加」。オフラインでも開ける。

**PC で開く（2026-09-14）**

- Chrome / Edge ではメニューに「PC にアプリとして追加」が出る。`beforeinstallprompt` を受け取った時だけ出し、一度使うと消える（イベントは一度きりしか使えない）。入れ済み・Safari・Firefox では出ない
- `コード帳を開く.bat` は **公開版を `msedge --app=` で開く**（無ければ Chrome、どちらも無ければ既定のブラウザ）。以前は開発サーバを立てて localhost を開いていたが、Python が要り、黒い窓が残り、**localhost と公開版は保存領域が別なので曲も別になる**ため変えた。開発中の確認は下の `devserver.py` を使う
- `--app` の窓も入れたアプリも、同じブラウザの公開版と保存領域は共通。Dropbox の接続は PC ごとに1回

⚠ **公開リポジトリなので、著作権のある歌詞を含むファイルを入れないこと。**
`.gitignore` で `.claude/*.png`（歌詞入りのテスト画像）を除外している。
曲データはファイルに含まれない（端末の localStorage のみ）。



追記（2026-09-12 夕方）。**歌詞行を切り出して1枚に積み直してまとめて認識**する方式に変えた。
1行ずつ `rectangle` で認識していたときは **1行あたり約1.05秒**かかり、27行の譜面で30秒近かかっていた。
まとめて読むことで **27行の譜面が 4.2 秒**になった。この形を崩すと使えない速さに戻るので注意。

#### 高精度モデル（jpn best・13MB）は入れない

試して比べた結果、**効果が無いどころか悪くなった**ので採用しない。
best の単一パスでは歌詞の一部が英字に化け（平仮名の連続が `Blac` のような英字列になる）、
fast＋2段構えのほうが正しく読めた。
**精度を決めているのはモデルでなく「歌詞行を日本語モデル単独で読み直す」方。**
退避先: `..\_アーカイブ\ギター_未使用_tessdata-best_2026-09-12`（2026-09-13 に退避済み）

#### 「疑わしいコード」の検出に OCR の確度は使えない

確度で範囲を切ると、`AonC#` `Bm7onA` のような**長いコード名が正しく読めていても並んでしまう**。
実測では 16 件並んで全部正解だった。固定値でも中央値比較でも改善しない。

代わりに使っているのは**音楽的なありえなさ**。

1. 出てくるコードのルートが一番よく收まる長音階を 12 通り総当たりで探す
2. 8割以上が收まる音階が見つかった時だけ、それを基準にする
3. **その音階から外れていて、かつ1〜2回しか出てこないコード**を疑わしいとする

誤読は「そこだけ調から外れる」形で現れるためこれが効く。
終止和音からのキー推定（`Sheet.guessKey`）を使うと、抜粋だとキーを外して誤検出を生むので使わない。

実測: 誤検出 **16件 → 0件**、真の検出は `BbonC`（♭を読み落とした箇所）を正しく1件だけ指摘。

#### 固有名詞は OCR では直らない。検索で直す

アーティスト名はカタカナの「ン」が「ソ」に化けるような誤読が残る。
一方で**曲名は文字が大きいので正確に読める**。だから曲名で検索してアーティスト名を引き当てる。

使っているのは **iTunes Search API**。`https://itunes.apple.com/search?country=JP&media=music&entity=song&term=...`
キー不要、CORS 許可済みでブラウザから直接呼べる。邦楽の収録も良い。
MusicBrainz も試したが 503 で使えなかった。

### OCR の実測（2026-09-12・合成画像 1020px 幅で計測）

| 項目 | 結果 |
|---|---|
| コード認識 | **28/28 正解**（`AonC#` `BbonC` `DonF#` `F#7` 等を含む） |
| 日本語の歌詞 | 3行中2行が完全一致。1行に4文字の誤り（jpn fast モデルの限界） |
| 処理時間 | 2.6〜5.5 秒（初回はエンジン読み込みで+数秒） |
| 確度 | 84% |

効いた工夫は次の3つ。**いずれも外すと精度が落ちるので注意。**

1. **2段構えの認識** — 1回目は `jpn+eng` で行の位置と種類を判定し、**歌詞行だけを `jpn` 単独で読み直す**（`refineLyrics`）。混在モデルだと小さい日本語が英字に化ける（平仮名6文字が `SUKBlac` という英字列になった）
2. **コード名を辞書に寄せる補正**（`snap`）— 誤読を1〜2文字の置き換えと1文字削除で探索し、`chords.js` の語彙に一致するものを採用。`8m7`→`Bm7`、`AZ`→`A7`、`AonCc#`→`AonC#` が直る
3. **割れたコードの結合**（`mergeSplitChords`）— `Bb` と `onc` のように分かれて読まれたものを、つなげて辞書に当たればまとめる

やって失敗したこと: OCR結果に `Sheet.clean()` をかけると、**誤読が1つ混ざった行をまるごと捨ててしまう**（イントロ2行が消えた）。OCR経路では刈り込まない。

読み落としやすいのは **♭ と ♯**。確度の低いコードは取り込み後に一覧で警告する。

撮り方で精度が変わる。**ダイアグラム表示OFF・文字サイズ中以上・譜面部分だけを範囲選択**が最良。

### 英語の歌詞と、コードの位置合わせ（2026-09-13）

**英語の歌詞で単語が全部つながる**（`Amazinggrace,howsweetthesound`）不具合を直した。原因は私の `pairToText` が歌詞の断片を**日本語の感覚で間を空けずに連結**していたこと。OCR 自体は単語の区切りを正しく認識していた。

- **空白の入れ方**（`needsSpace`）: 日本語どうしは入れない。英字どうしは OCR が空白でしか英単語を区切らないので、枠が離れていれば空白。日本語と英字の境目は、日本語の文字が枠に左右の余白を含む（空白ありでも実測 5px、h=28）ので低めの基準 h×0.10
- **英語は eng 単独で読み直す**。混在モデルだと `I` が `1` や `|` に、`Don't` が `Dontt` になる。日英混在の行は jpn で読んだあと、英単語の枠だけ eng の読みに差し替える。単独の `1` `|` `l` は `I` に直す（`fixEnglishWords`）
- ⚠ **jpn での読み直しは「全部の歌詞行」にかける。** 「日本語を含む行だけ」に絞ると、最初の読み取り（混在モデル）が小さい日本語を英字に化けさせた行（`振り向かないで` → `HUA AGUS`）を英語の行と取り違え、日本語が消えて `Don't look back FRUMIDBWE` になった。**言語の判定は読み直した後の中身で行う**
- ⚠ **OCR が返す日本語の文字の枠は不正確。** 1文字ぶんずれる、極端に広い、後ろの文字まで覆う、が普通に起きる（`日` の枠が本来より1文字右、`向` の枠が後ろの「かないで」まで覆う等）。**英単語の枠と、行全体の左右の端は正確。** そのためコードの位置は、英単語は枠の中を文字数で割り、日本語は**すき間で区切った連なりごとに端から端までを均等に割って**求める。すき間の基準は h×0.75
- コードが**歌詞の末尾より右**にあれば歌詞のあとに置く。以前は最後の単語に吸い寄せて2つ目を単語の途中に押し込み、`so und` のように単語が割れて見えた

**検証は「正解つき画像」で行う。** フォントから文字ごとの位置を実測し、コードを狙った文字の真上に正確に置いた画像を作り、表示と同じ `Sheet.parse` を通して「コードの下に来た文字」が正解と一致するかを数える。すき間なし5行（`ocr-truth`）＋すき間あり4行（`ocr-truth-cells`）で計36コード。**枠をそのまま使うと、すき間なしの画像で 15/20（外れた5件はすべて日本語で、すべて1文字左）。均等割りにしたら両方の画像で 36/36。**
雑に作ったテスト画像（コードが文字の真上にない）では、位置が変わっても正しいかどうか判断できないので、位置合わせの変更はこの正解つき画像で確かめること。

テスト用の画像と正解データは `.claude/` にある（Dropbox で同期、Git には入れない）。

#### 歌詞をネットで検索して直す仕組みは作らない

曲名・アーティスト名は**事実**なので検索してよいが、**歌詞は著作物**。無料の歌詞データベース（LRCLIB 等）は許諾なしに歌詞を載せており、**それと知りながらダウンロードすると私的使用でも違法**（著作権法30条1項4号。2021年1月から全ての著作物が対象・文化庁）。許諾のある歌詞 API（Musixmatch 等）は無料枠で一部しか返さず保存も禁止。**画面に出ているものを自分で OCR するのとは性質が違う。** 精度はモデルの読み直しと辞書的な補正で上げる。

### 分数コードの表記

`AonC#` のような書き方は**そのまま表示する**（本人の希望）。`chords.js` の `parse` が区切り（`/` か `on`）を `sep` として覚え、`build` で復元する。転調しても `BonD#` のように `on` のまま。押さえ方図の辞書は `/` 表記で引く。
## 実装のクセ・注意点

### メトロノーム（2026-09-13）

- **発音は Web Audio の時刻で先に予約する**（25ms ごとに 0.1 秒先まで）。setInterval で直接鳴らすとリズムがよれる
- 裏に回ってタイマーが大きく遅れたら、**溜まった拍を一気に鳴らさず捨てて合わせ直す**（`resyncs`）
- 実測: 120 BPM で予約間隔 **500.000ms・誤差 0ms**
- ⚠ **リズムを `performance.now()` と見た目の点滅で測ってはいけない**。点滅は requestAnimationFrame に乗っているので、ブラウザが裏にいる間は止まり、再開時に溜まった拍が一気に出る。これで平均間隔 159ms（本当は 250ms）と誤って出た。音の正確さは `Metronome._stats().scheduled`（オーディオの時計）で測る
- iPhone は AudioContext をユーザー操作の中で作る必要がある。消音スイッチ対策に `navigator.audioSession.type = 'playback'`（対応端末のみ）
- **タップテンポ**: 2秒あけたら数え直し、直近8間隔を使い、中央値から25%以上ずれた間隔（叩き損ね）は除外して平均。実測で 900ms の叩き損ねや 250ms の早打ちが1回混ざっても 120 BPM のまま
- テンポと拍数は曲に `bpm` / `beats` として保存（同期にも乗る）

### 同期（2026-09-13）

- **Dropbox アプリ: App key `2isrviyoa3hd7j2`**（`sync.js` の `APP_KEY` に組み込み済み。公開してよい値）。アクセスの種類は App folder。**App secret は使わないし、リポジトリに入れてはいけない**
- 登録済みの Redirect URI: `https://ks-kit.github.io/chord-book/` と `http://localhost:8766/`（2026-09-13 に Dropbox の認可画面がエラーを出さないことで確認）。公開先の URL を変えたら Dropbox 側にも追加すること

- **認証は PKCE**（秘密鍵なし）。パラメータは Dropbox 公式 SDK `dropbox-sdk-js/src/auth.js` で確認した。トークン交換でも `client_id` は必須で、`client_secret` の代わりに `code_verifier` を送る。`token_access_type=offline` で更新用トークンが取れ、更新時も `client_id` だけで秘密鍵は要らない
- ⚠ **認可で `redirect_uri` を付けたら交換でも同じ値を付ける。付けなかったら交換でも付けない。** 食い違うと拒否される
- ⚠ **iPhone のホーム画面アプリは Safari と保存領域が別。** Dropbox から戻る先が Safari 側になると、アプリ側に保存した verifier が読めず接続できない。そのため**コードを貼り付けて接続する方式**も用意し、ホーム画面から開いている時はそちらを案内する（`Sync.isStandalone()`）
- `Dropbox-API-Arg` ヘッダは ASCII しか通らないので、それ以外は Unicode エスケープにする（公式 SDK の `httpHeaderSafeJson` と同じ）
- **まとめ方**: 曲ごとに `updatedAt` が新しいほうを採用。同時刻は手元優先。**削除は `{id, deleted:true, updatedAt}` の削除印に置き換えて残す**（消すと他の端末に伝わらない）。90日で掃除
- 書き込みは Dropbox の `rev` を指定した update。他の端末が先に書いていたら 409 conflict が返るので、読み直してまとめ直す（最大4回）
- 変更が無ければ書き込まない（`signature` で比較）
- 保存・削除のたびに 2.5 秒待ってから同期（続けて編集しても1回にまとまる）。アプリに戻った時も同期（20秒あける）
- 同期中に手元で編集されても消さないよう、受け取った結果は**今の手元ともう一度まとめてから**反映する

検証は**偽の Dropbox サーバ**（`window.fetch` を差し替え）で2台の端末を模擬して行った。初回送信・合流・受信のみ・編集と削除の伝達・変更なしで書かない・**書き込み衝突からの復帰**・**401 からの自動復帰**の8場面すべて通過。**2026-09-13、本人のアカウントで本物の Dropbox と接続し、端末間の同期が動くことを確認済み。**

### コード行が歌詞として表示される問題（2026-09-13）

**OCR 側と表示側でコード行の判定が食い違っていた。** OCR 側（`ocr.js` の `classify`）は「コードが6割以上ならコード行」とゆるく判定するが、表示側（`parser.js` の `isChordLine`）は「コードでも記号でもない語が1つでもあれば歌詞行」と厳しかった。OCR で1つだけ崩れたコード（`onc`、`AonCc#`、`Em7|`）が残ると、**その行がまるごと歌詞として表示された。**

- `isChordLine` を「コードが2つ以上・6割以上で、**英単語らしい語が無ければ**コード行」に改めた。英単語らしさは小文字が3つ以上続くか（`looksLikeWord`）。ただし `onC` `onc` のような「on＋ベース音」は分数コードの読み崩れなので単語とみなさない
- 読めなかった語は消さずに、**薄い赤の点線で位置どおり表示**する（`seg__ch--unread`）。直す場所だと分かるように
- `Em7|` のように小節線がくっついた語は線を外して読む（`stripWrap`）

検証: 崩れを含むコード行 8/8 をコード行と判定、**紛らわしい英語の歌詞 23/23 を歌詞のまま判定**（`Am I dreaming` `Em, I miss you` `Bb Bb Baby` `A Day in the Life` 等）。コード位置の正解つき画像 36/36、英語の読み取りも変化なし。

#### 1文字違いの読み崩れ（`F#aud` → `F#aug`）（2026-09-13）

`F#aug` が `F#aud` と読まれ、`aud` が小文字3連続なので**英単語とみなされ、行ごと歌詞にされた**（実際のスクショで発生）。

- `Chords.nearest` を追加。根音のあとの部分が、既知の品質（`aug` `sus4` `maj7` …）と**1文字違い（置換・挿入・削除）で、候補がちょうど1つ**のときだけ直す
- **3文字未満は直さない。** 2文字だと `Aim` → `Adim` のような当て推量が出た
- 候補が2つ以上・遠いものは直さない（`Dmai7` `Gsu4` `Cadd8` `Emit` `Amid` は赤の点線のまま）。間違ったコードを出すより、直す場所として見せるほうが良い
- 使い道は3か所: OCR の `snap` の最後、`looksLikeWord`（直せる語・`#` を含む語は単語でない）、表示時の `extractChords`
- 表示時に直したコードは本文を書き換えず、**いつもの色で点線の下線**（`seg__ch--guessed`）。PC ではマウスを乗せると元の綴りが出る（iPhone では出ない）。保存済みの曲も、開くだけでこう表示される

検証: `F#aud`→`F#aug`、`Asud4`→`Asus4`、`Caud`→`Caug`。コード行 10/10・歌詞行 30/30・位置 36/36・英語 40語でコードへの誤変換なし。

#### `.gitignore` の追記チェックは行単位で

「`*.png` が既に書いてあるか」を部分文字列で調べると、`.claude/*.png` に一致して**直下の画像の除外が追加されなかった。** 置いてもらったスクショには歌詞が写っているので、直下の画像は `/*.png` 等で除外し `!/icon-*.png` だけ戻している。

### iPhone での表示の詰め方（2026-09-13）

iPhone（375×812）で実測してから直した。

| | 変更前 | 変更後 |
|---|---|---|
| コードの文字 | **14.3px**（歌詞 17px より小さかった） | ルート **19.0px**、`m7` `onA` 等は 13.3px |
| コードの右余白 | 10px（0.7em） | 0.34em |
| `Bm7onA` の横幅 | 69px | 66px（大きくしたのに縮んだ） |
| ツールバー | **5段・296px（画面の36%）** | **3段・157px** |
| 譜面が見える高さ | 459px | **597px** |

- **コードは「ルート（大）＋残り（小）」に分けて描く**（`fillChordLabel`）。弾きながら一番見たいのはルート。表記そのもの（`AonC#`）は変えない。タップ判定は外側の `.seg__ch--tap` で取るので、内側の文字を押しても押さえ方が出る
- ツールバーは「転調・カポ」「BPM」「文字・送り」の3段。拍の点は ♩ ボタンの下のふちに入れて1段節約。補足の1行は空なら出さない
- 狭いボタンに入る値の最悪値を確認した。全角カッコだと `Ebm（+10）` が 79px の枠から1px はみ出したので、**転調の表示は半角カッコ**にした
- ツールバーを畳んだ状態は `prefs.toolbarCollapsed` に覚える

### ⚠ `const` で宣言したモジュールは `window` から見えない

`Chords` `Sheet` `Ocr` `Metronome` `TapTempo` `Sync` は `const X = (() => {...})()` で宣言している。**`const` はグローバルでも `window` のプロパティにならない**ので、`window.Sync` は常に `undefined`。存在チェックは `typeof Sync === 'undefined'` で書く。2026-09-13 にこれで**3件の不具合**を出した（自動同期が一度も走らない／同期表示が出ない／曲を離れてもメトロノームが止まらない）。

### それ以前からの注意点

- **貼り付けの自動抽出**は `parser.js` の `clean()`。コード行が密集している塊を「譜面」とみなし、24行以上離れたコード行は別の塊として切る。範囲外を捨て、範囲内でも `NOISE_RE` に当たる行（「広告」「関連曲」など単独の語）は落とす。**サイト固有の作りに依存しない**のが狙い
- コード譜サイトからの自動取得（サーバが機械的に取りに行く形）は入れない。規約に触れるうえ、ブラウザからは CORS で直接読めない。「自分が開いているページを自分用にコピーする」までが線引き
- ⚠ **バックスラッシュのエスケープ（改行・タブ・復帰の記法）が、ヒアドキュメント経由で書くと実文字に化ける**ことがある。2026-09-12 に `clean()` で発生し、構文エラーで `Sheet` ごと読み込まれなくなった。正規表現やエスケープを含む JS は **Write ツールで書く**か、書いた後に `cat -A` で必ず確認する
- **コード行の判定**は品質のホワイトリスト方式（`chords.js` の `QUALITIES`）。ここに無い綴りはコードとみなさない。歌詞行を誤ってコード行と判定しないための作り。新しい表記で誤判定が出たらここに足す
- **押さえ方図**の解決順は、辞書（`OPEN_SHAPES`）→ E型／A型のバレー生成。そのあと分数コードならベースを組み込む
- **分数コードはベースを必ず鳴らす**（`applyBass`）。以前は辞書にある `D/F#` だけ再現し、
  `GonA` などはベースを捨てて本体で代用していた。現在は計算で作る。注意点が3つある。
  1. **「低い」は弦の番号でなく実際の音の高さで見る**。
     5弦6フレット(Eb)より4弦開放(D)のほうが低いので、番号で判定すると `DonEb` が壊れる。`OPEN_MIDI` で比べる
  2. **ベース弦を置き換えてルートが消えたら、上の弦で補う**（`ensureRoot`）。`Am7onD` で発生
  3. **`ensureRoot` はベースを押さえている弦を絶対に触ってはいけない**。触るとベースが消える（`F9onE` で発生）

  選び方は「ルートが残る」を絶対条件にし、あとは弾きやすさで採点する。
  弦の本数を重く見すぎると `GonA` が「6弦5フレット＋Gの形」という押さえにくい形になるので、
  **開放弦でベースが出せることを厚めに評価し、指の開きを減点**している。

  実測（2026-09-12）: 12ルート × 14クオリティ × 12ベース = **1848通りを全数検算して NG 0 件**。
  （ベースが最低音か、ルートが含まれるか、3弦以上鳴るか、指の開きが4フレット以内か）
  6弦で物理的に無理な `FdimonEb` の1件だけ `exact:false` でベースを省く。画面にその旨を出す。
- **バレー線**は形状から推測せず、生成元（`OPEN_BARRE` とバレー生成）が持っている情報で描く。形だけで判定すると D や Bm7 に誤ってバレー線が出る
- **キー推定**は終止和音（譜面上で最後に鳴るコード）を見ている。素朴な判定なので外すことはある
- ⚠ **コードが付かない枠（コードより前から始まる歌詞）には、消えない空白（U+00A0）を入れる。**
  ふつうの半角スペースだと CSS の空白処理で消え、その枠に行ボックスができず高さが 0 になる。
  `align-items: baseline` の基準がずれて、**その歌詞だけ約5.8px 上にずれる**（2026-09-12 に発生）。
  幅を増やさないため `.seg--nochord .seg__ch` に `width: 0; overflow: hidden; padding-right: 0` をあてている。
  直したあとの実測: 27行でずれ 0 件。
- 曲データの構造: `{id, title, artist, body, transpose, capo, font, createdAt, updatedAt}`。`body` は貼り付けた原文をそのまま持つ（表示のたびに解析する）
